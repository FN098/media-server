# アプリを外部公開する場合、以下のドメインを取得済みのドメインに変更し、
# ルータのポートフォワーディングを設定する
your.domain.com {

  rate_limit {
    zone global {
      key {remote_ip}
      window 10s
      events 20
    }
  }

  reverse_proxy app:3000
}
